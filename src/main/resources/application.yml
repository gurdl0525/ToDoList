spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DBurl}
    password: ${password}
    username: ${userName}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none